using System;

namespace DentalDrill.CRM.Models
{
    public static class JobTypes
    {
        public const String Estimate = nameof(JobTypes.Estimate);

        public const String Sale = nameof(JobTypes.Sale);
    }
}
