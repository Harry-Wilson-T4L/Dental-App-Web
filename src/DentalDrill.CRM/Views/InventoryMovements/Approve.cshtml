@using DevGuild.AspNetCore.Controls.HybridForms
@using DentalDrill.CRM.Extensions
@model DentalDrill.CRM.Models.ViewModels.InventoryMovements.InventoryMovementApproveModel
@{
    ViewBag.Title = "Approve Move";
    this.ConfigureHybridPage("InventoryMovementsApprove");
}

<div class="editor">
    @if (!this.Context.IsAjaxRequest())
    {
        <div class="editor__header">
            <div class="editor__title">
                <h1>Approve Move</h1>
            </div>
            <div class="editor__actions">
                <a asp-action="Index" asp-controller="InventoryMovements" class="editor__action"><span class="fas fa-list"></span> List</a>
            </div>
        </div>
    }

    <form asp-action="Approve" asp-route-id="@Model.Movement.Id" hybrid-form-id="InventoryMovementsApprove" id="InventoryMovementsApproveForm">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="row">
            <div class="col-12 mb-2">
                <div class="row">
                    <div class="col-12">
                        <label>SKU</label>
                    </div>
                    <div class="col-12">
                        <p class="form-control-plaintext">[@Model.SKU.Type.Name] @Model.SKU.Name</p>
                    </div>
                </div>
            </div>

            <div class="col-12 col-sm-6 mb-2">
                <div class="row">
                    <div class="col-12">
                        <label>Type</label>
                    </div>
                    <div class="col-12">
                        <p class="form-control-plaintext">@Model.Movement.Type.ToDisplayName()</p>
                    </div>
                </div>
            </div>

            <div class="col-12 col-sm-6 mb-2">
                <div class="row">
                    <div class="col-12">
                        <label>Quantity</label>
                    </div>
                    <div class="col-12">
                        <p class="form-control-plaintext">@Model.Movement.QuantityAbsolute.ToString("#,##0.##")</p>
                    </div>
                </div>
            </div>
        </div>

        <hr />

        <div class="row">
            <div class="col-12 mb-2">
                <div class="row">
                    <div class="col-12">
                        <label>Comment</label>
                    </div>
                    <div class="col-12">
                        <p class="form-control-plaintext">@Model.Movement.Comment</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col editor__submit">
                <button type="submit" class="btn btn-primary">Approve</button>
                <a asp-action="Index" asp-controller="InventoryMovements" class="btn btn-outline-secondary ml-2 editor__submit__cancel">Cancel</a>
            </div>
        </div>
    </form>
</div>